services:
  - type: worker
    name: pdf-myanmar-bot
    env: python
    buildCommand: pip install -r requirements.txt && playwright install chromium --with-deps
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
